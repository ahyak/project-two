<% provide(:title, "Home") %>
<h1>All the posts</h1>
<ul>
  <% @posts.each do |post| %>
    <li> <%= link_to post.title, post_path(post.id) %> </li>
    <li><h4> Created by: <%= link_to post.user.first_name, user_path(post.id) %></h4></li>
  <% end %>
</ul>
